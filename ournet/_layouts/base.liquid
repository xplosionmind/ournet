<!DOCTYPE html>
<html
	lang='{{ lang | default: 'en' }}'
	dir='ltr'
	prefix='og: http://ogp.me/ns#'
>

	{% if reference_page %}{% comment %}If the page has the reference_page value, it means that it has a translation, or that it is the translation of another page{% endcomment %}
		{% assign alternates = '' | split: '/' %}{% comment %}Creating an empty array{% endcomment %}
		{% for entry in collections.all %}{% comment %}Loop through all pages of the website{% endcomment %}
			{% if entry.data.reference_page == reference_page and entry.data.lang != lang %}{% comment %}Check if the entry has the same reference_page value of the current page, and if it has a different language{% endcomment %}
				{% assign alternates = alternates | concat: entry %}{% comment %}If previous conditions apply, add the entry to the array of alternates of this page{% endcomment %}
			{% endif %}
		{% endfor %}
	{% endif %}

	{% render 'head.liquid', date: date, updated: updated, title: title, description: description, lang: lang, alternates: alternates, site: site, url: page.url %}

	<body{% if class %} class='{{ class }}'{% endif %}>
	<div class='red disclaimer center'>
		<h2>⚠️ Disclaimer ⚠️</h2>
		<p>This website is under heavy development, both in terms of code and content.<br>
			Please, <strong>do not share it</strong> yet, and consider anything on here as <strong>ephemeral</strong> and <strong>incomplete</strong>.</p>
		<p><strong>You are welcome to <a href='/contribute/'>contribute</a>!</strong> Have a look at the <a href='https://github.com/users/xplosionmind/projects/3' title='Knitting Our Internet – Roadmap · GitHub'>roadmap</a> to learn more</a>.</p>
	</div>

	{% include 'header.liquid', lang: lang %}

		{{ content }}
		{% render 'footer.liquid', lang: lang, source: site.source, path: page.inputPath, url: page.url, eleventyVersion: eleventy.version %}
	</body>
</html>
